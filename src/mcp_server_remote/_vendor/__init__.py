# __init__.py
# In-tree ("vendored") EXTERNAL dependencies. Nothing under _vendor/ was hand-written by
# this repository's author - each package documents its own origin in its README.md.
# Vendored pip-style so the project stays zero-install and the code's origin stays obvious.
